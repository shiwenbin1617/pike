import React from 'react';
import LoadingSpinner from '../components/LoadingSpinner';

const Loading = () => {
  return (
    <div>
      <LoadingSpinner />
    </div>
  );
};

export default Loading;
